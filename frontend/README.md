# DocuQuery frontend

React + Vite UI for the PDF RAG API.

```bash
cp .env.example .env
npm install
npm run dev
```

Requires the API at `VITE_API_BASE_URL` (default `http://localhost:8000`). See the root [README](../README.md).
